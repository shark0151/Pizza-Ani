<Page
    x:Class="Pizza_Ani_Time.View.ContactUs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pizza_Ani_Time.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="25,100,25,100">
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="41*"/>
                <RowDefinition Height="41*"/>
                <RowDefinition Height="103*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="47*"/>
                <ColumnDefinition Width="98*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Padding="20,0,20,0">
                <TextBlock Text="Name:" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top"  Margin="0,10,0,0"/>
            </Grid>
            <RichEditBox   Grid.Column="1" x:Name="NameLabel" AutomationProperties.Name="Simple Text Editor" Margin="10,10,10,100" />

            <Grid Grid.Row="1" Padding="20,0,20,0">
                <TextBlock Text="E-mail:" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0"/>
            </Grid>
            <RichEditBox  Grid.Row="1" Grid.Column="1" x:Name="EmailLabel" AutomationProperties.Name="Simple Text Editor" Margin="10,10,10,100" />

            <Grid Grid.Row="2" Padding="20,0,20,0" Margin="0,0,0,248">
                <TextBlock Text="Message:" Grid.Row="2" Grid.Column="0" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>
            <RichEditBox  Grid.Row="2" Grid.Column="1" x:Name="MessageLabel" AutomationProperties.Name="Simple Text Editor" Margin="10,10,10,248" />
        </Grid>



        <Grid Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Stretch">
            <Button Content="Send" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Background="Green" Click="SendMail_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <!--<Button Content="Button" Grid.Row="2" Grid.Column="1" Background="Red" Click="Button_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>-->
        </Grid>



    </Grid>

</Page>
